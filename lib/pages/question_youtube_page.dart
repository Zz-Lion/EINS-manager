import 'package:flutter/material.dart';

class QuestionYoutubePage extends StatelessWidget {
  const QuestionYoutubePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
